class Friendship < ApplicationRecord
  
    # belongs_to :User, :foreign_key => "user_id", :class_name => "user"
    # belongs_to :Friendship, :foreign_key => "friend_id", :class_name => "user"  

end
